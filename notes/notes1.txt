🔥 Video-11 : How Versioning Works in NodeJS?
 "express": "^5.2.1"
1st part ->5 major update -there might be changes that it might break when we update be carefull when u update
2nd part ->2 Reccomended security/bug fix
3rd part ->1 minor fixes(optional update)
what is carrot symbol ?
this means(5)the first no is locked by default it wont update that only 2nd and 3rd it'll update
carrot symbol means install all recomended and minor fixes automatically when i do npm install 
~ - if u use this dont change recommended version change only last version (first 2 wont change only last one will change)

🔥 Video-12 : What is REST API?
there is server and client(alexa,browser,mobile) 
req and res will happen as we know 
1.It'll woek on server client archeture - means both client and server are diffrent entites both should be independent 
they shouldn't depend on each other
2.data is JSON format frontend will deicde what to do with data
3.always respect all http methods 
we have already made note of this

🔥 Video-13 : Building REST API's using Node and Express.js (file -video13.js)
Here our idea is to make REST API -json
GET /users - List all users
GET /users/2 - Get the users with a particular idea
POST /users -create new users
Patch /users/1 -Edit the user with ID 1
DELETE /users/1 - Delete the user with ID 1
Here im using hardcoded data since there is no DB
🔥 Video-14 : Introduction to POSTMAN for REST API's  (file -video13.js)
Postman is the tool used for API testing and documentation


🔥 Video-15 : Express Middleware  (file -video13.js)
Middleware is between the req and res it validates the incomming req. The req has to go through the middleware
req->middleware->res (this is the steps,we can have multiple middlewares also)

🔥 Video-16 : What are HTTP Headers in API
Here we jusyt created a custom header (res.setHeader("x-myname","roidon"))
🔥 Video-17 : HTTP Status Codes
🔥 Video-18 : Getting Started with MongoDB
MongoDB NO sql document database
strong suppport for aggretion pipes
works on bison format
Best for Node applications
In sql we have tables but in mongodb we have collections
Inside collections we have documents
Mongodb commands
show dbs - shows the DB
use <db_name> to use a particular DB
show collections 
db.coll.find 
db.coll.insert

🔥 Video-19 : Connecting NodeJS with MongoDB | Mongoose + Express(file : video14.js)
Till now we used to use MOCK_DATA.json file as DB

I run the command in cmd(first run mongosh)

1. test> show dbs
admin           40.00 KiB
config         108.00 KiB
ecommerce        8.00 KiB
firstDB         72.00 KiB
local           40.00 KiB
youtube-app-1   12.00 KiB
test>

2. test> use youtube-app-1
switched to db youtube-app-1
youtube-app-1>

3.youtube-app-1> show collections
users
youtube-app-1>
after you post the data run the find command you'll see the data(db.users.find({}))

🔥 Video-20 : Model View Controller in NodeJS | MVC Pattern
Controller manuplates the model and model updates the View